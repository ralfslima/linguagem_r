# Vetor de nomes
vetor <- c("Alice", "Bruno", "Carla", "Daniel")

# Exibir o primeiro nome
print(vetor[1])

# Exibir todos os nomes
print(vetor)

# Percorrer o vetor
for(nome in vetor){
    print(nome)
}